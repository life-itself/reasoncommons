---
title: TODO — 2R Research Group talk
updated: 2026-09-01
---

# TODO

Open tasks for [`Where Does the Thinking Go?`](./README.md). Most urgent first.

TODO: @David — pick the forum thread for **Demo B**. Need one real, public Second Renaissance forum thread, 4 to 6 posts, the kind of exchange that normally just scrolls away without connecting to anything. Best case: one post supports one of the eight goal-tree conditions, one challenges a link between conditions, and one raises a claim the tree currently can't place, so the reader demo has something for each of its three card types. The forum is public, so no permissions issue. Drop the link here when picked.

TODO: **Demo C staging** — attempt it live with the room, but build a pre-prepared mock-up beforehand (a set of contributions already collected and run through fit / gap-or-disagreement / so what, plus the reverse view) to fall back to if the live run stalls or the AI placement is poor on the day. Decide live-vs-mock on the day; the mock-up ships regardless.

TODO: build the **reverse-view visual** — the tree of change with the currently least-supported condition highlighted and an annotation count on each of the eight. Needed for Demo A's payoff and Demo C step 4.

TODO: capture **static fallback screenshots** from the tool before the talk — full tree; one leaf-path highlighted; one link glossed; the chosen forum thread; the reader's placement cards; the reverse view.

TODO: confirm **Demo C kit** — shared form for ~1,000-character contributions, QR code + short link, screen operator, 3 to 4 minutes of quiet write time.

TODO: after the slides brief is signed off, **build the deck** — frontend-slides skill, Life Itself warm-earth template as the starting point (`~/src/life-itself/design/presentations/life-itself-warm-earth-reference.html`).

TODO: **image research pass** against the "Assets to source" list in [`slides-brief.md`](./slides-brief.md). Not blocking; every slide works type-only.
